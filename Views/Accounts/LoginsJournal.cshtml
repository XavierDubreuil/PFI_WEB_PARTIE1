@model ChatManager.Models.Login
@{
    ViewBag.Title = "LoginsJournal";
}

<h2>Journal des connexions</h2>
<hr />
<div id="LoginsContainer" class=" LoginsContainer"> <!-- Refreshed periodically --> </div>
@Scripts.Render("~/bundles/js")
@section Scripts {
    <script defer>
        initTimeout(120);

        //let logins = new PartialRefresh("@Url.Action("GetUsersList")", "UsersListContainer", 5
        let logins = new PartialRefresh("@Url.Action("Logins", "Accounts")", "LoginsContainer", 5);
    </script>
}



